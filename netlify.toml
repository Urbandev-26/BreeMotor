[build]
  command = "echo 'No build step required'"
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@netlify/blobs"]

[[redirects]]
  from = "/service-login"
  to = "/service-login.html"
  status = 200
